apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "{{ syncthing.server.name }}-data"
  namespace: "{{ common.namespace }}"
  labels:
    {{ service.extraLabels | to_nice_yaml | indent(4) }}
spec:
  storageClassName: "{{ common.storage.class }}"
  resources:
    requests:
      storage: "{{ syncthing.server.dataStorage.capacity }}"
  accessModes:
    - ReadWriteMany
  volumeName: "{{ common.namespace }}-{{ syncthing.server.name }}-data"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "{{ syncthing.server.name }}-files"
  namespace: "{{ common.namespace }}"
  labels:
    {{ service.extraLabels | to_nice_yaml | indent(4) }}
spec:
  storageClassName: "{{ common.storage.class }}"
  resources:
    requests:
      storage: "{{ syncthing.server.filesStorage.capacity }}"
  accessModes:
    - ReadWriteMany
  volumeName: "{{ common.namespace }}-{{ syncthing.server.name }}-files"
