apiVersion: v1
kind: Secret
metadata:
  name: "{{ gitlab.cache.name }}"
  namespace: "{{ common.namespace }}"
  labels:
    {{ service.extraLabels | to_nice_yaml | indent(4) }}
type: "Opaque"
data:
  accessKey: "{{ gitlab.cache.accessKey | b64encode }}"
  secretKey: "{{ gitlab.cache.secretKey | b64encode }}"
