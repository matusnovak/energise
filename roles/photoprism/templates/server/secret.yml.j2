apiVersion: v1
kind: Secret
metadata:
  name: "{{ photoprism.server.name }}"
  namespace: "{{ common.namespace }}"
  labels:
    {{ service.extraLabels | to_nice_yaml | indent(4) }}
type: "Opaque"
data:
  databasePassword: "{{ photoprism.server.database.password | b64encode }}"
