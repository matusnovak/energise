apiVersion: v1
kind: Secret
metadata:
  name: "{{ nextcloud.redis.name }}"
  namespace: "{{ common.namespace }}"
  labels:
    {{ service.extraLabels | to_nice_yaml | indent(4) }}
type: "Opaque"
data:
  password: "{{ nextcloud.redis.password | b64encode }}"
