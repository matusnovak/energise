apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{ traefik.server.name }}"
  namespace: "{{ common.namespace }}"
  labels:
    {{ service.extraLabels | to_nice_yaml | indent(4) }}
data:
  "traefik.yml": |
    {{ traefik_yml | indent(4) }}
