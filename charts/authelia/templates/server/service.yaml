{{ include "service-spec" (dict 
  "Values" .Values
  "Release" .Release
  "Chart" .Chart
  "component" "server"
  "service" .Values.authelia.server.service) }}
  ports:
    - port: 9091
      protocol: "TCP"
      name: "web"
