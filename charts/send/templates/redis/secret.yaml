apiVersion: v1
kind: Secret
{{ include "metadata-spec" (dict 
  "Values" .Values
  "Release" .Release
  "Chart" .Chart
  "component" "redis") }}
type: "Opaque"
data:
  password: "{{ .Values.redis.password | b64enc }}"
