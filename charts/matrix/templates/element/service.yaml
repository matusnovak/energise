{{ include "service-spec" (dict 
  "Values" .Values
  "Release" .Release
  "Chart" .Chart
  "component" "element"
  "service" .Values.element.service) }}
  ports:
    - port: 80
      protocol: "TCP"
      name: "web"
