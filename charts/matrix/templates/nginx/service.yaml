{{ include "service-spec" (dict 
  "Values" .Values
  "Release" .Release
  "Chart" .Chart
  "component" "nginx"
  "service" .Values.nginx.service) }}
  ports:
    - port: 80
      protocol: "TCP"
      name: "web"
