{{ include "deployment-spec" (dict 
  "Values" .Values
  "Release" .Release
  "Chart" .Chart
  "component" "krokimermaid") }}
spec:
  replicas: 1
  strategy:
    type: Recreate
  revisionHistoryLimit: 0
  {{- include "selector-spec" (dict "Chart" .Chart "component" "krokimermaid") | nindent 2 }}
  template:
    {{- include "metadata-spec" (dict 
      "Values" .Values
      "Release" .Release
      "Chart" .Chart
      "component" "krokimermaid"
      "annotations" .Values.krokimermaid.pod.annotations) | nindent 4}}
    spec:
      serviceAccountName: "{{ .Chart.Name }}-krokimermaid"
      securityContext:
        runAsUser: {{ .Values.krokimermaid.uid }}
        runAsGroup: {{ .Values.krokimermaid.gid }}
        fsGroup: {{ .Values.krokimermaid.gid }}
      {{- with .Values.krokimermaid.pullSecret }}
      imagePullSecrets:
        - name: "{{ . }}"
      {{- end }}
      containers:
        - name: "{{ .Chart.Name }}-krokimermaid"
          image: "{{ .Values.krokimermaid.image.name }}:{{ .Values.krokimermaid.image.tag }}"
          imagePullPolicy: "{{ .Values.krokimermaid.image.pullPolicy }}"
          {{- with .Values.krokimermaid.env }}
          env:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          ports:
            - name: web
              containerPort: 8002
              protocol: TCP
          resources:
            {{- toYaml .Values.krokimermaid.resources | nindent 12 }}
          securityContext:
            allowPrivilegeEscalation: false
          volumeMounts:
            - name: "data-volume"
              mountPath: "/data"
      nodeSelector:
        {{- toYaml .Values.krokimermaid.nodeSelector | nindent 8 }}
      volumes:
        - name: "data-volume"
          emptyDir: {}
