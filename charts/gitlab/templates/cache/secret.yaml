apiVersion: v1
kind: Secret
{{ include "metadata-spec" (dict 
  "Values" .Values
  "Release" .Release
  "Chart" .Chart
  "component" "cache") }}
type: "Opaque"
data:
  accessKey: "{{ .Values.gitlab.cache.accessKey | b64enc }}"
  secretKey: "{{ .Values.gitlab.cache.secretKey | b64enc }}"
