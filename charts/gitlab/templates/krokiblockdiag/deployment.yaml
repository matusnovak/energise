{{ include "deployment-spec" (dict 
  "Values" .Values
  "Release" .Release
  "Chart" .Chart
  "component" "krokiblockdiag") }}
spec:
  replicas: 1
  strategy:
    type: Recreate
  revisionHistoryLimit: 0
  {{- include "selector-spec" (dict "Chart" .Chart "component" "krokiblockdiag") | nindent 2 }}
  template:
    {{- include "metadata-spec" (dict 
      "Values" .Values
      "Release" .Release
      "Chart" .Chart
      "component" "krokiblockdiag"
      "annotations" .Values.krokiblockdiag.pod.annotations) | nindent 4}}
    spec:
      serviceAccountName: "{{ .Chart.Name }}-krokiblockdiag"
      securityContext:
        runAsUser: {{ .Values.krokiblockdiag.uid }}
        runAsGroup: {{ .Values.krokiblockdiag.gid }}
        fsGroup: {{ .Values.krokiblockdiag.gid }}
      {{- with .Values.krokiblockdiag.pullSecret }}
      imagePullSecrets:
        - name: "{{ . }}"
      {{- end }}
      containers:
        - name: "{{ .Chart.Name }}-krokiblockdiag"
          image: "{{ .Values.krokiblockdiag.image.name }}:{{ .Values.krokiblockdiag.image.tag }}"
          imagePullPolicy: "{{ .Values.krokiblockdiag.image.pullPolicy }}"
          {{- with .Values.krokiblockdiag.env }}
          env:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          ports:
            - name: web
              containerPort: 8001
              protocol: TCP
          resources:
            {{- toYaml .Values.krokiblockdiag.resources | nindent 12 }}
          securityContext:
            allowPrivilegeEscalation: false
          volumeMounts:
            - name: "data-volume"
              mountPath: "/data"
      nodeSelector:
        {{- toYaml .Values.krokiblockdiag.nodeSelector | nindent 8 }}
      volumes:
        - name: "data-volume"
          emptyDir: {}
