{{ include "deployment-spec" (dict 
  "Values" .Values
  "Release" .Release
  "Chart" .Chart
  "component" "krokiblockdiag") }}
spec:
  replicas: 1
  strategy:
    type: Recreate
  revisionHistoryLimit: 0
  {{- include "selector-spec" (dict "Chart" .Chart "component" "krokiblockdiag") | nindent 2 }}
  template:
    {{- include "metadata-spec" (dict 
      "Values" .Values
      "Release" .Release
      "Chart" .Chart
      "component" "krokiblockdiag"
      "annotations" .Values.gitlab.krokiblockdiag.pod.annotations) | nindent 4}}
    spec:
      serviceAccountName: "{{ .Chart.Name }}-krokiblockdiag"
      securityContext:
        runAsUser: {{ .Values.gitlab.krokiblockdiag.uid }}
        runAsGroup: {{ .Values.gitlab.krokiblockdiag.gid }}
        fsGroup: {{ .Values.gitlab.krokiblockdiag.gid }}
      {{- with .Values.gitlab.krokiblockdiag.pullSecret }}
      imagePullSecrets:
        - name: "{{ . }}"
      {{- end }}
      containers:
        - name: "{{ .Chart.Name }}-krokiblockdiag"
          image: "{{ .Values.gitlab.krokiblockdiag.image.name }}:{{ .Values.gitlab.krokiblockdiag.image.tag }}"
          imagePullPolicy: "{{ .Values.gitlab.krokiblockdiag.image.pullPolicy }}"
          {{- with .Values.gitlab.krokiblockdiag.env }}
          env:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          ports:
            - name: web
              containerPort: 8001
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: web
            failureThreshold: 3
            initialDelaySeconds: 5
            periodSeconds: 10
            successThreshold: 1
            timeoutSeconds: 2
          readinessProbe:
            httpGet:
              path: /
              port: web
            failureThreshold: 1
            initialDelaySeconds: 5
            periodSeconds: 10
            successThreshold: 1
            timeoutSeconds: 2
          resources:
            {{- toYaml .Values.gitlab.krokiblockdiag.resources | nindent 12 }}
          securityContext:
            allowPrivilegeEscalation: false
          volumeMounts:
            - name: "data-volume"
              mountPath: "/data"
      nodeSelector:
        {{- toYaml .Values.gitlab.krokiblockdiag.nodeSelector | nindent 8 }}
      volumes:
        - name: "data-volume"
          emptyDir: {}
