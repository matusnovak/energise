{{ include "deployment-spec" (dict 
  "Values" .Values
  "Release" .Release
  "Chart" .Chart
  "component" "krokibpmn") }}
spec:
  replicas: 1
  strategy:
    type: Recreate
  revisionHistoryLimit: 0
  {{- include "selector-spec" (dict "Chart" .Chart "component" "krokibpmn") | nindent 2 }}
  template:
    {{- include "metadata-spec" (dict 
      "Values" .Values
      "Release" .Release
      "Chart" .Chart
      "component" "krokibpmn"
      "annotations" .Values.krokibpmn.pod.annotations) | nindent 4}}
    spec:
      serviceAccountName: "{{ .Chart.Name }}-krokibpmn"
      securityContext:
        runAsUser: {{ .Values.krokibpmn.uid }}
        runAsGroup: {{ .Values.krokibpmn.gid }}
        fsGroup: {{ .Values.krokibpmn.gid }}
      {{- with .Values.krokibpmn.pullSecret }}
      imagePullSecrets:
        - name: "{{ . }}"
      {{- end }}
      containers:
        - name: "{{ .Chart.Name }}-krokibpmn"
          image: "{{ .Values.krokibpmn.image.name }}:{{ .Values.krokibpmn.image.tag }}"
          imagePullPolicy: "{{ .Values.krokibpmn.image.pullPolicy }}"
          {{- with .Values.krokibpmn.env }}
          env:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          ports:
            - name: web
              containerPort: 8003
              protocol: TCP
          resources:
            {{- toYaml .Values.krokibpmn.resources | nindent 12 }}
          securityContext:
            allowPrivilegeEscalation: false
          volumeMounts:
            - name: "data-volume"
              mountPath: "/data"
      nodeSelector:
        {{- toYaml .Values.krokibpmn.nodeSelector | nindent 8 }}
      volumes:
        - name: "data-volume"
          emptyDir: {}
