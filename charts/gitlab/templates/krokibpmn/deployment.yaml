{{ include "deployment-spec" (dict 
  "Values" .Values
  "Release" .Release
  "Chart" .Chart
  "component" "krokibpmn") }}
spec:
  replicas: 1
  strategy:
    type: Recreate
  revisionHistoryLimit: 0
  {{- include "selector-spec" (dict "Chart" .Chart "component" "krokibpmn") | nindent 2 }}
  template:
    {{- include "metadata-spec" (dict 
      "Values" .Values
      "Release" .Release
      "Chart" .Chart
      "component" "krokibpmn"
      "annotations" .Values.gitlab.krokibpmn.pod.annotations) | nindent 4}}
    spec:
      serviceAccountName: "{{ .Chart.Name }}-krokibpmn"
      securityContext:
        runAsUser: {{ .Values.gitlab.krokibpmn.uid }}
        runAsGroup: {{ .Values.gitlab.krokibpmn.gid }}
        fsGroup: {{ .Values.gitlab.krokibpmn.gid }}
      {{- with .Values.gitlab.krokibpmn.pullSecret }}
      imagePullSecrets:
        - name: "{{ . }}"
      {{- end }}
      containers:
        - name: "{{ .Chart.Name }}-krokibpmn"
          image: "{{ .Values.gitlab.krokibpmn.image.name }}:{{ .Values.gitlab.krokibpmn.image.tag }}"
          imagePullPolicy: "{{ .Values.gitlab.krokibpmn.image.pullPolicy }}"
          {{- with .Values.gitlab.krokibpmn.env }}
          env:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          ports:
            - name: web
              containerPort: 8003
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: web
            failureThreshold: 3
            initialDelaySeconds: 5
            periodSeconds: 10
            successThreshold: 1
            timeoutSeconds: 2
          readinessProbe:
            httpGet:
              path: /
              port: web
            failureThreshold: 1
            initialDelaySeconds: 5
            periodSeconds: 10
            successThreshold: 1
            timeoutSeconds: 2
          resources:
            {{- toYaml .Values.gitlab.krokibpmn.resources | nindent 12 }}
          securityContext:
            allowPrivilegeEscalation: false
          volumeMounts:
            - name: "data-volume"
              mountPath: "/data"
      nodeSelector:
        {{- toYaml .Values.gitlab.krokibpmn.nodeSelector | nindent 8 }}
      volumes:
        - name: "data-volume"
          emptyDir: {}
