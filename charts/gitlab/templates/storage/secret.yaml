apiVersion: v1
kind: Secret
{{ include "metadata-spec" (dict 
  "Values" .Values
  "Release" .Release
  "Chart" .Chart
  "component" "storage") }}
type: "Opaque"
data:
  accessKey: "{{ .Values.gitlab.storage.accessKey | b64enc }}"
  secretKey: "{{ .Values.gitlab.storage.secretKey | b64enc }}"
