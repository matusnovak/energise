{{ include "deployment-spec" (dict 
  "Values" .Values
  "Release" .Release
  "Chart" .Chart
  "component" "kroki") }}
spec:
  replicas: 1
  strategy:
    type: Recreate
  revisionHistoryLimit: 0
  {{- include "selector-spec" (dict "Chart" .Chart "component" "kroki") | nindent 2 }}
  template:
    {{- include "metadata-spec" (dict 
      "Values" .Values
      "Release" .Release
      "Chart" .Chart
      "component" "kroki"
      "annotations" .Values.gitlab.kroki.pod.annotations) | nindent 4}}
    spec:
      serviceAccountName: "{{ .Chart.Name }}-kroki"
      securityContext:
        runAsUser: {{ .Values.gitlab.kroki.uid }}
        runAsGroup: {{ .Values.gitlab.kroki.gid }}
        fsGroup: {{ .Values.gitlab.kroki.gid }}
      {{- with .Values.gitlab.kroki.pullSecret }}
      imagePullSecrets:
        - name: "{{ . }}"
      {{- end }}
      containers:
        - name: "{{ .Chart.Name }}-kroki"
          image: "{{ .Values.gitlab.kroki.image.name }}:{{ .Values.gitlab.kroki.image.tag }}"
          imagePullPolicy: "{{ .Values.gitlab.kroki.image.pullPolicy }}"
          {{- with .Values.gitlab.kroki.env }}
          env:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          ports:
            - name: web
              containerPort: 8000
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: web
            failureThreshold: 3
            initialDelaySeconds: 5
            periodSeconds: 10
            successThreshold: 1
            timeoutSeconds: 2
          readinessProbe:
            httpGet:
              path: /
              port: web
            failureThreshold: 1
            initialDelaySeconds: 5
            periodSeconds: 10
            successThreshold: 1
            timeoutSeconds: 2
          resources:
            {{- toYaml .Values.gitlab.kroki.resources | nindent 12 }}
          securityContext:
            allowPrivilegeEscalation: false
          volumeMounts:
            - name: "data-volume"
              mountPath: "/data"
      nodeSelector:
        {{- toYaml .Values.gitlab.kroki.nodeSelector | nindent 8 }}
      volumes:
        - name: "data-volume"
          emptyDir: {}
