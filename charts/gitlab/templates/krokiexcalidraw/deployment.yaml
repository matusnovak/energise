{{ include "deployment-spec" (dict 
  "Values" .Values
  "Release" .Release
  "Chart" .Chart
  "component" "krokiexcalidraw") }}
spec:
  replicas: 1
  strategy:
    type: Recreate
  revisionHistoryLimit: 0
  {{- include "selector-spec" (dict "Chart" .Chart "component" "krokiexcalidraw") | nindent 2 }}
  template:
    {{- include "metadata-spec" (dict 
      "Values" .Values
      "Release" .Release
      "Chart" .Chart
      "component" "krokiexcalidraw"
      "annotations" .Values.krokiexcalidraw.pod.annotations) | nindent 4}}
    spec:
      serviceAccountName: "{{ .Chart.Name }}-krokiexcalidraw"
      securityContext:
        runAsUser: {{ .Values.krokiexcalidraw.uid }}
        runAsGroup: {{ .Values.krokiexcalidraw.gid }}
        fsGroup: {{ .Values.krokiexcalidraw.gid }}
      {{- with .Values.krokiexcalidraw.pullSecret }}
      imagePullSecrets:
        - name: "{{ . }}"
      {{- end }}
      containers:
        - name: "{{ .Chart.Name }}-krokiexcalidraw"
          image: "{{ .Values.krokiexcalidraw.image.name }}:{{ .Values.krokiexcalidraw.image.tag }}"
          imagePullPolicy: "{{ .Values.krokiexcalidraw.image.pullPolicy }}"
          {{- with .Values.krokiexcalidraw.env }}
          env:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          ports:
            - name: web
              containerPort: 8004
              protocol: TCP
          resources:
            {{- toYaml .Values.krokiexcalidraw.resources | nindent 12 }}
          securityContext:
            allowPrivilegeEscalation: false
          volumeMounts:
            - name: "data-volume"
              mountPath: "/data"
      nodeSelector:
        {{- toYaml .Values.krokiexcalidraw.nodeSelector | nindent 8 }}
      volumes:
        - name: "data-volume"
          emptyDir: {}
