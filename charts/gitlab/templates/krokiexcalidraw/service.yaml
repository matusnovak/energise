{{ include "service-spec" (dict 
  "Values" .Values
  "Release" .Release
  "Chart" .Chart
  "component" "krokiexcalidraw"
  "service" .Values.gitlab.krokiexcalidraw.service) }}
  ports:
    - port: 8004
      protocol: "TCP"
      name: "web"
