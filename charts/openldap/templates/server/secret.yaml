apiVersion: v1
kind: Secret
{{ include "metadata-spec" (dict 
  "Values" .Values
  "Release" .Release
  "Chart" .Chart
  "component" "server") }}
type: "Opaque"
data:
  password: "{{ .Values.openldap.server.password | b64enc }}"
  readonlyPassword: "{{ .Values.openldap.server.readonlyPassword | b64enc }}"
