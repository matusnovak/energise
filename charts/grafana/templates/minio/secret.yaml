apiVersion: v1
kind: Secret
{{ include "metadata-spec" (dict 
  "Values" .Values
  "Release" .Release
  "Chart" .Chart
  "component" "minio") }}
type: "Opaque"
data:
  accessKey: "{{ .Values.grafana.minio.accessKey | b64enc }}"
  secretKey: "{{ .Values.grafana.minio.secretKey | b64enc }}"
