{{ include "service-spec" (dict 
  "Values" .Values
  "Release" .Release
  "Chart" .Chart
  "component" "server"
  "service" .Values.prometheus.server.service) }}
  ports:
    - port: 9090
      protocol: "TCP"
      name: "web"
