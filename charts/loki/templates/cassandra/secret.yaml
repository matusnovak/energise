apiVersion: v1
kind: Secret
{{ include "metadata-spec" (dict 
  "Values" .Values
  "Release" .Release
  "Chart" .Chart
  "component" "cassandra") }}
type: "Opaque"
data:
  password: "{{ .Values.cassandra.password | b64enc }}"
