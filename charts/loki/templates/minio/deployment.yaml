{{ include "deployment-spec" (dict 
  "Values" .Values
  "Release" .Release
  "Chart" .Chart
  "component" "minio") }}
spec:
  replicas: 1
  strategy:
    type: Recreate
  revisionHistoryLimit: 0
  {{- include "selector-spec" (dict "Chart" .Chart "component" "minio") | nindent 2 }}
  template:
    {{- include "metadata-spec" (dict 
      "Values" .Values
      "Release" .Release
      "Chart" .Chart
      "component" "minio"
      "annotations" .Values.minio.pod.annotations) | nindent 4}}
    spec:
      serviceAccountName: "{{ .Chart.Name }}-minio"
      securityContext:
        runAsUser: {{ .Values.minio.uid }}
        runAsGroup: {{ .Values.minio.gid }}
        fsGroup: {{ .Values.minio.gid }}
      {{- with .Values.minio.pullSecret }}
      imagePullSecrets:
        - name: "{{ . }}"
      {{- end }}
      containers:
        - name: "{{ .Chart.Name }}-minio"
          image: "{{ .Values.minio.image.name }}:{{ .Values.minio.image.tag }}"
          imagePullPolicy: "{{ .Values.minio.image.pullPolicy }}"
          command:
            - 'sh'
          args:
            - '-c'
            - |
              id;
              stat "/export";
              mkdir -p "/export/${MINIO_BUCKET_NAME}";
              minio server "/export";
          env:
            - name: "MINIO_ROOT_USER"
              valueFrom:
                secretKeyRef:
                  name: "{{ .Chart.Name }}-minio"
                  key: "accessKey"
                  optional: false
            - name: "MINIO_ROOT_PASSWORD"
              valueFrom:
                secretKeyRef:
                  name: "{{ .Chart.Name }}-minio"
                  key: "secretKey"
                  optional: false
            - name: "MINIO_BROWSER"
              value: "off"
            - name: "MINIO_BUCKET_NAME"
              value: "objects"
            - name: "MINIO_USERNAME"
              value: "minio"
            - name: "MINIO_GROUPNAME"
              value: "minio"
          {{- with .Values.minio.env }}
            {{- toYaml . | nindent 12 }}
          {{- end }}
          ports:
            - name: s3
              containerPort: 9000
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /minio/health/live
              port: s3
            failureThreshold: 3
            initialDelaySeconds: 5
            periodSeconds: 10
            successThreshold: 1
            timeoutSeconds: 2
          readinessProbe:
            httpGet:
              path: /minio/health/live
              port: s3
            failureThreshold: 1
            initialDelaySeconds: 5
            periodSeconds: 10
            successThreshold: 1
            timeoutSeconds: 2
          resources:
            {{- toYaml .Values.minio.resources | nindent 12 }}
          securityContext:
            allowPrivilegeEscalation: false
          volumeMounts:
            - name: "data-volume"
              mountPath: "/export"
      nodeSelector:
        {{- toYaml .Values.minio.nodeSelector | nindent 8 }}
      volumes:
        - name: "data-volume"
          persistentVolumeClaim:
            claimName: "{{ .Chart.Name }}-minio-data"
