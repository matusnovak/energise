{{ include "service-spec" (dict 
  "Values" .Values
  "Release" .Release
  "Chart" .Chart
  "component" "server"
  "service" .Values.server.service) }}
  ports:
    - port: 7777
      protocol: "TCP"
      name: "web"
