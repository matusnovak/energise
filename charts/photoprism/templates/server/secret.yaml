apiVersion: v1
kind: Secret
{{ include "metadata-spec" (dict 
  "Values" .Values
  "Release" .Release
  "Chart" .Chart
  "component" "server") }}
type: "Opaque"
data:
  databasePassword: "{{ .Values.server.database.password | b64enc }}"
  adminUsername: "{{ .Values.server.auth.username | b64enc }}"
  adminPassword: "{{ .Values.server.auth.password | b64enc }}"
