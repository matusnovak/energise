{{ include "service-spec" (dict 
  "Values" .Values
  "Release" .Release
  "Chart" .Chart
  "component" "server"
  "service" .Values.phpldapadmin.server.service) }}
  ports:
    - port: 80
      protocol: "TCP"
      name: "web"
